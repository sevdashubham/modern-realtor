export interface IFormDataKey {
    [key: string]: any;
}

export interface IFormInput {
    datePrepared: string;
    firstName: string;
    lastName: string;
    streetAddress: string;
    purchasePrice: string;
    zipCode: number;
    city: string;
    county: string;
}
